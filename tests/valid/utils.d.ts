import CustomDate from '../../lib/customDate';
/**
 * @description
 *  Transform the Date data value
 *  to the CustomDate class instance lookalike
 *
 * @export
 * @param {Date} value
 * @returns {CustomDate}
 */
export default function (value: Date): CustomDate;
